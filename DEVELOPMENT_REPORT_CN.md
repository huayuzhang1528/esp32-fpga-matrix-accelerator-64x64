# ESP32 + FPGA 矩阵加速器工程复盘报告

## 1. 报告目的

本文记录从原始版本到当前 64×64 最终版本的完整开发过程，包括：

- 为什么需要修改原方案；
- 实际修改了哪些 ESP32 与 FPGA 代码；
- 每个问题是如何定位的；
- 哪些方案尝试后失败，以及失败提供了什么信息；
- 如何从测量结果决定下一步，而不是凭感觉优化；
- 当前版本能够诚实声称什么，不能声称什么；
- 以后解决类似软硬件协同问题时，可以复用哪些方法。

报告范围从最初可靠但很慢的 100 kHz SPI 版本开始，一直覆盖到当前已经写入 FPGA 内置 Flash、支持最大 64×64、冷启动后可直接运行的版本。

开发过程中保留了原始工程，没有覆盖原始版本。当前仓库包含最终工程。

虽然根目录仍保留 `4x4` 名称，但当前逻辑矩阵最大尺寸已经是 64×64。`4x4` 来自最初的物理计算阵列设计历史。

---

## 2. 最终结果摘要

### 2.1 最终硬件与数据格式

- FPGA：Gowin `GW1NSR-LV4CQN48PC6/I5`
- ESP32：ESP32-D0WD-V3，240 MHz
- 输入：signed INT8
- 累加与输出：signed INT32
- 支持尺寸：4 到 64，且为 4 的倍数
- 最终 SPI 请求频率：19 MHz
- FPGA 物理计算资源：8 个 `MULTADDALU18X18` DSP，使用器件全部 16 个乘法器
- B/权重矩阵保存在 FPGA BSRAM 中
- A 每次流式发送两行
- FPGA 每次返回一个 2×4 的 INT32 结果块
- ESP32 与 FPGA 之间使用 SPI Mode 0 和独立 DONE 信号

### 2.2 最终资源占用

| 资源 | 使用量 |
|---|---:|
| Logic | 1012 / 4608，22% |
| Registers | 1220 / 3573，35% |
| BSRAM | 8 / 10，80% |
| DSP | 8 / 8，100% |

### 2.3 最终实测性能

以下 FPGA 时间包含 A 传输、FPGA 计算、DONE 等待和 C 回读；B 首次加载单独列出。

| 矩阵 | B 首次加载 | ESP32 软件计算 | FPGA resident-B 路径 | 加速比 |
|---:|---:|---:|---:|---:|
| 8×8 | 0.048 ms | 0.050 ms | 0.268 ms | 0.188× |
| 16×16 | 0.145 ms | 0.389 ms | 1.035 ms | 0.376× |
| 24×24 | 0.313 ms | 1.298 ms | 2.286 ms | 0.568× |
| 32×32 | 0.549 ms | 3.061 ms | 4.019 ms | 0.762× |
| 40×40 | 0.854 ms | 5.958 ms | 6.252 ms | 0.953× |
| 48×48 | 1.229 ms | 10.276 ms | 8.952 ms | 1.148× |
| 56×56 | 1.668 ms | 16.294 ms | 12.131 ms | 1.343× |
| 64×64 | 2.170 ms | 24.296 ms | 15.838 ms | 1.534× |

当前 64×64 通用版本的性能交叉点在 40×40 与 48×48 之间。

64×64 即使把一次性的 B 加载也计算进去：

```text
15.838 ms + 2.170 ms = 18.008 ms
24.296 ms / 18.008 ms ≈ 1.35×
```

因此：

- B 可复用时，64×64 实测约 1.53×；
- 单次连 B 一起加载时，仍约 1.35×；
- 小矩阵不能声称加速，因为通信和事务开销占主导。

---

## 3. 性能比较到底比较了什么

ESP32 软件参考不是调用某个“ESP32 自带矩阵乘法器”，而是在 ESP32 上运行普通三重循环：

```cpp
for (row)
    for (col)
        for (k)
            C[row][col] += A[row][k] * B[k][col];
```

它的作用有两个：

1. 作为正确性基准，逐元素比较 FPGA 输出；
2. 作为软件执行时间基准，与完整 FPGA 路径比较。

项目同时保留了三种命令：

- `test N`：CPU 计算、FPGA 计算并逐元素比较；
- `bench N`：先验证结果，再测 CPU 与 FPGA 时间；
- `run N`：只运行 FPGA 并输出校验和，不做 CPU 比较和性能输出。

这种分离很重要。演示命令、正确性命令和性能命令承担不同职责，避免“为了显示加速比而在正式路径里加入不必要的 CPU 计算”。

---

## 4. 第一阶段：建立独立版本和可靠基线

### 4.1 为什么先另建文件

最初要求是不覆盖已经能够工作的旧工程，因此创建了独立的 FPGA RTL、Arduino sketch、约束和位流文件，并保留：

- 原始项目；
- 100 kHz 可靠位流；
- 32×32 高速位流；
- 当前 64×64 最终位流。

这使每次高风险实验都可以回退。调试硬件时，“保留一个已知可工作版本”比尽快修改主文件更重要。

### 4.2 初始矩阵结构

最初采用 4×4 C tile：

- B 常驻 FPGA；
- ESP32 每次发送四行 A；
- FPGA 使用 8 个双乘加 DSP，在两个行阶段中完成 4×4 输出；
- 每个 4×4 tile 返回 16 个 INT32，即 64 字节。

矩阵最大尺寸最初限制为 32×32。

### 4.3 100 kHz 基线

首先没有直接追求高速，而是在 100 kHz 下确认：

- signed INT8 输入正确；
- signed INT32 累加正确；
- 4、8、16、32 尺寸全部逐元素一致；
- SPI 命令、DONE 和结果字节序正确。

初始测量：

| 尺寸 | ESP32 | FPGA | 加速比 |
|---:|---:|---:|---:|
| 4×4 | 0.007 ms | 6.783 ms | 0.001× |
| 8×8 | 0.050 ms | 26.869 ms | 0.002× |
| 16×16 | 0.389 ms | 106.950 ms | 0.004× |
| 32×32 | 3.061 ms | 426.889 ms | 0.007× |

结论不是“FPGA计算慢”，而是“SPI传输完全淹没了计算收益”。这一步把后续工作的目标明确为：先提高可靠通信带宽，再讨论计算阵列。

---

## 5. 第二阶段：定位 SPI 无法提速的原因

### 5.1 现象

使用原逻辑引脚映射时：

- 100 kHz 正确；
- 提高到 250 kHz、500 kHz、1 MHz 或更高后开始出现确定性错误；
- 即使做简单的 MOSI→MISO 临时 loopback，高频也会损坏。

这说明问题不在矩阵乘法算法，因为 loopback 根本不经过矩阵计算。

### 5.2 错误假设和排除过程

最开始可能怀疑：

- 杜邦线接错；
- SPI Mode 错误；
- 结果字节序错误；
- DONE 信号逻辑错误；
- FPGA 运算时序错误。

但用户确认接线没有变化，旧项目前几天可以工作；100 kHz 下完整计算也正确。于是重点从“接线是否正确”转向“这些物理引脚是否适合高速时钟”。

### 5.3 查器件引脚数据库，而不是继续猜

检查 Gowin 本地器件数据库后发现：

- FPGA pin 31：`GCLKC_3`
- FPGA pin 32：`GCLKT_3`
- FPGA pin 42：`GCLKC_1`
- FPGA pin 40：普通 I/O

原来的物理线没有错，但原 SCLK 所在的逻辑路径不适合作为可靠高速时钟输入。

### 5.4 不改实体线，改逻辑角色

实体接线保持不变，但在 ESP32 与 FPGA 代码中交换这些线承担的角色：

| 信号 | ESP32 | FPGA pin |
|---|---:|---:|
| SCLK | GPIO 4 | 42 |
| MISO | GPIO 18 | 40 |
| MOSI | GPIO 23 | 31 |
| CS | GPIO 5 | 39 |
| DONE | GPIO 19 | 32 |

这样做的关键思路是：

> “线没有移动”不等于“信号角色不能重新定义”。只要每根线两端的软件与约束同时修改，就能把最适合时钟的物理通道分配给 SCLK。

### 5.5 显式使用全局时钟网络

仅把 SCLK 放到 clock-capable pin 还不够。在 RTL 中显式加入：

```verilog
BUFG sclk_global_buffer (.I(sclk), .O(sclk_global));
```

这样 SCLK 才真正进入 FPGA 的专用全局时钟网络，而不是被综合器作为普通逻辑信号布线。

### 5.6 SPI 接收跨时钟域重构

高速版本没有在板载 `clk` 域中边检测 SCLK 边沿，而是：

1. 在原生 `sclk_global` 上升沿直接采样 MOSI；
2. 每完成一个字节，翻转一个 toggle；
3. 将 toggle 同步到 FPGA 板载 `clk` 域；
4. 板载时钟域检测 toggle 变化并消费稳定的字节寄存器。

这是典型的 CDC 思路：数据保持稳定，用同步后的事件通知另一个时钟域，而不是把多位数据每一位分别同步。

### 5.7 失败尝试：把 SCLK 放在 pin 31，同时保留 pin 32 作为 MISO

pin 31/32 属于同一个全局时钟差分资源对。该组合并没有得到可靠高速结果。之后把 SCLK 放到 pin 42，并把 MISO 移到 FPGA pin 40 对应的现有实体线，高速正确性才明显改善。

这个失败说明：只看到某个引脚标有 `GCLK` 并不代表周围所有原信号组合都适合当前用途，还要考虑配对资源和实际路由。

---

## 6. 第三阶段：32×32 高速专用版本

### 6.1 从 1 MHz 到 10 MHz

完成引脚角色重分配、BUFG 和原生 SCLK 接收后：

- 1 MHz、5 MHz、10 MHz 逐步通过；
- 4、8、16、32 全尺寸都进行逐元素比较；
- 10 MHz 下 32×32 FPGA 路径降到约 5.47 ms，但仍未超过 ESP32 的 3.06 ms。

### 6.2 优化 ESP32 SPI 事务管理

最初每个短事务反复创建和关闭 SPI transaction，固定软件开销很大。修改为：

- 频率变化时才重新配置 SPI bus；
- 普通命令仅拉低/拉高 CS；
- 避免每个 tile 反复 `beginTransaction/endTransaction` 的框架开销。

这类优化不改变总字节数，却能显著减少大量短事务的固定成本。

### 6.3 20 MHz 的确定性首字节错误

20 MHz 下，4/8/16 多数正确，但 32×32 的第一个结果出现稳定错误，例如期望 `-72`、实际 `-8`。稳定重复的相同错误比随机错误更有价值，因为它通常指向固定的位时序，而不是噪声。

检查后发现 MISO 首位在第一下降沿准备时存在竞争。解决方案是：

- 完成计算后保存稳定的结果快照；
- 第一个字节直接从快照驱动；
- 后续位继续使用下降沿移位逻辑。

修改后 20 MHz 下 32×32 连续通过。

### 6.4 没有效果的尝试：只增加一个 MISO 输出寄存器

曾尝试单纯给 MISO 增加一级输出寄存器，但 20 MHz 的固定错误没有变化。原因是问题位于“首位何时准备”而不是“最终引脚有没有寄存器”。

经验：增加寄存器并不是通用时序修复。必须明确寄存器放在哪个时钟域、解决的是建立时间还是协议首位初始化。

### 6.5 40 MHz 失败

40 MHz 下所有尺寸产生稳定的多位错位和错误结果，因此没有用错误数据计算加速比，也没有把偶尔通过当成成功。

### 6.6 22–26 MHz 看似通过但没有变快

ESP32 命令后来允许输入任意 1–40 MHz。测试发现请求 22、24、25、26 MHz 都能通过，但耗时与 20 MHz 完全相同；27 MHz 开始与 40 MHz 一样失败。

这说明 ESP32 SPI 硬件分频不是连续的。20–26 MHz 请求落在同一实际时钟档位，27 MHz 跳到下一个过高档位。

经验：API 参数不等于实际硬件频率。判断是否真的提速要看测量值，而不能只看设置值。

### 6.7 去掉每个 CS 事务两侧的 1 μs 等待

当时距离 1× 只差约 0.23 ms。统计事务数量后发现，每个事务前后各 1 μs 的等待累计已经足以解释差距。

去掉等待后进行了多次完整正确性验证，结果仍稳定。32×32 最终得到：

```text
ESP32：3.061 ms
FPGA：2.955 ms
加速比：1.036×
```

这是旧 32×32 专用固件的性能交叉点。

---

## 7. 第四阶段：从 32×32 扩展到 64×64

### 7.1 为什么考虑 64×64

32×32 只有约 1.036×，虽然已经是真实端到端加速，但优势太小。矩阵乘法计算量按 `N³` 增长，而输入输出传输量主要按 `N²` 增长，因此提高 N 有机会让计算收益逐渐超过通信开销。

### 7.2 第一版 64×64 扩展

最初保留四行 A、4×4 结果 tile，仅扩大：

- B bank：每个从 128 bytes 增加到 512 bytes；
- A bank：每个从 16 bytes 增加到 32 bytes；
- B 地址从 7 bit 增加到 9 bit；
- A 地址从 4 bit 增加到 5 bit；
- K pair 计数器从 4 bit 增加到 5 bit；
- `MAX_N` 从 32 增加到 64。

B 总共 4096 bytes，仍可放入原来的 8 个 BSRAM；真正困难的是 A 为了同时提供多端口读取，被综合成大量寄存器。

### 7.3 失败结果：布局还差 354 个寄存器

第一版通过了 RTL 解析和综合，但布局失败：

```text
Failed to place with '354 REG(s) unPlaced'
```

报告显示大约：

- Logic：51%
- Registers：78%
- BSRAM：80%
- DSP：100%

总寄存器百分比看起来没有超过 100%，但 FPGA slice 中 LUT/FF 的可配对位置、局部路由和固定资源限制会导致“理论数量够，实际放不下”。

经验：不能只看资源总数；必须真正完成 placement and routing。

### 7.4 最终架构决策：两行 A、2×4 tile

观察物理 DSP 结构后发现，8 个双乘加 DSP 每周期本来就只同时处理两行、四列的 8 个输出；旧方案为了返回 4×4 tile，把另外两行保存在寄存器中，再运行第二个 row phase。

因此将协议改为：

- 每次只发送两行 A；
- 每次计算和返回 2×4 结果；
- 删除 A2/A3 四个 bank；
- 删除 C 下半部分 8 个 INT32 累加寄存器；
- ESP32 的 `rowBase` 从每次加 4 改为每次加 2；
- 每个 tile 回读从 64 bytes 改为 32 bytes。

代价是事务次数增加，但没有减少每周期使用的乘法器数量。资源下降到：

- Logic：22%
- Registers：34–35%
- BSRAM：80%
- DSP：100%

随后成功完成综合、布局、布线和位流生成。

### 7.5 为什么当前 32×32 又慢了

这是旧版和当前版数据看似矛盾的原因：

| 特性 | 旧 32×32 专用版 | 当前 64×64 通用版 |
|---|---:|---:|
| A 每次发送 | 4 行 | 2 行 |
| 返回 tile | 4×4 | 2×4 |
| 稳定 SPI 设置 | 20 MHz | 19 MHz |
| 32×32 FPGA 时间 | 2.955 ms | 4.019 ms |
| 32×32 加速比 | 1.036× | 0.762× |
| 最大矩阵 | 32×32 | 64×64 |

两组数据都正确，但属于不同固件，不能混合描述。

---

## 8. 第五阶段：64×64 结果发送时序

### 8.1 失败尝试：直接从 256-bit 结果总线按位选择

为了节省寄存器，第一版 2×4 发送器直接使用：

```text
result_stream[255 - bit_position]
```

这样避免了完整 256-bit 移位副本，但综合后形成很深的动态多路选择器。

硬件结果：

- 10 MHz：4、8、16、32、64 全部正确；
- 20 MHz：所有尺寸出现确定性的结果位组合错误。

因为降频后计算结果全部正确，可以把矩阵核心、存储地址和累加逻辑排除，问题集中在 MISO 输出路径。

### 8.2 成功方案：8-bit 逐字节发送器

最终发送器采用：

- 第一个字节直接驱动，保证第一个上升沿前数据有效；
- 后续结果按 byte index 选择；
- 在前一个字节结束的下降沿，把下一个字节装入 8-bit shift register；
- 字节内部只进行 8-bit 移位。

这样把 256:1 动态 bit mux 变为字节边界上的选择加 8-bit 快速移位，既保留了低寄存器占用，也改善了 MISO 时序。

### 8.3 20 MHz 仍有少量算术差异

改进发送器后，20 MHz 下多数结果字节恢复正确，但仍出现少量、稳定的算术差异；10 MHz 下所有尺寸完全正确。

此时错误特征从“大量位错位”变为“少量结果数值差几”，更像扩大 BSRAM 和重新布局后，MOSI 接收路径在 20 MHz 边缘丢失少量输入字节。

随后扫描 12–19 MHz：

- 12、13、14、15、16、17、18、19 MHz 请求全部通过 64×64；
- 20 MHz 出现确定性错误。

最终选择 19 MHz，不使用边缘不可靠的 20 MHz。

经验：最终频率应该留有正确性证据，而不是选择工具允许输入的最大数字。

---

## 9. 冷启动、Flash 和首次命令超时

### 9.1 SRAM 编程与内置 Flash 编程

开发阶段使用 Gowin Programmer 的 operation 2：

```text
SRAM Program
```

它适合快速测试，但断电后丢失。

最终使用 operation 6：

```text
embFlash Erase, Program, Verify
```

工具完成：

- 擦除内置 Flash；
- 写入最终 64×64 `.fs` 位流；
- 逐位校验；
- 返回 `Verify Success`。

随后执行 operation 1 `Reprogram`，强制 FPGA 从内置 Flash 重新加载，验证配置来源不是之前的 SRAM 状态。

### 9.2 真正断电测试发现首次命令超时

用户拔掉并重新连接开发板后：

- 第一条 `test 64` 超时；
- 紧接着 `bench 64` 正确并达到约 1.53×；
- 后续所有 `test 64` 都正确。

这证明：

- FPGA Flash 启动成功；
- 矩阵计算没有损坏；
- 问题只发生在冷启动后的第一条短 SPI 事务同步。

### 9.3 修复：启动时发送两次无害的 SET_N

ESP32 `setup()` 中在初始化后发送两次：

```cpp
fpgaConfigure(4);
fpgaConfigure(4);
```

如果 FPGA SPI 跨时钟同步器丢弃第一条短事务，第二条仍能建立 parser 状态。它们发生在用户命令之前，也不计入 benchmark 的矩阵执行时间。

修复后再次执行：

1. FPGA 从 Flash `Reprogram`；
2. ESP32 正常复位；
3. 第一条 `test 64`；
4. `bench 64`。

结果第一条测试直接通过，加速比仍约 1.53×。

---

## 10. 所有主要失败尝试及其价值

| 尝试 | 为什么尝试 | 实际结果 | 得到的结论 |
|---|---|---|---|
| 保持原 SPI 引脚直接提频 | 最小修改 | 250–500 kHz 起 loopback 已错误 | 问题在物理时钟/引脚路径，不在矩阵算法 |
| 只检查接线 | 最常见故障来源 | 100 kHz 全功能正确，实体线未变化 | 接线正确不代表引脚适合高速时钟 |
| SCLK 使用 pin 31、MISO 保持 pin 32 | pin 31/32 标有全局时钟资源 | 仍不可靠 | 差分配对资源和周围信号组合也会影响结果 |
| 只给 MISO 加一级寄存器 | 常见时序修复 | 20 MHz 固定首字节错误不变 | 首位初始化竞争不能靠随意加寄存器解决 |
| 40 MHz | 希望获得明显加速 | 多位确定性错误 | 不用错误结果计算加速比 |
| 请求 22–26 MHz | 希望刚好超过性能门槛 | 正确但耗时与 20 MHz 完全相同 | SPI 硬件分频离散，设置值不等于实际值 |
| 64×64 保留四行 A | 最大程度保留旧协议效率 | PnR 失败，354 个 REG 无法放置 | 必须根据真实物理资源重构架构 |
| 256-bit 结果总线直接动态选位 | 节省移位寄存器 | 10 MHz 正确，20 MHz 错误 | 大型动态 mux 成为 MISO 时序瓶颈 |
| 64×64 继续使用 20 MHz | 希望保留旧版频率 | 少量稳定算术差异 | 深 BSRAM 布局后的输入路径已经在边缘 |
| 只做 Flash verify，不做启动测试 | 工具已显示成功 | 冷启动第一条命令仍超时 | 编程成功不等于系统级启动协议正确 |

失败实验不是浪费。只要每次实验有单一问题、明确预期和可重复结果，它就在缩小故障范围。

---

## 11. 实际使用的工具和操作

### 11.1 Gowin FPGA 编译

没有依赖 Gowin IDE 窗口，而是调用：

```text
gw_sh.exe build_ESP32_FPGA_4x4_64x64.tcl
```

Tcl 脚本完成：

- 选择 `GW1NSR-LV4CQN48PC6/I5`；
- 加载 `.v` 和 `.cst`；
- 运行 synthesis；
- 运行 placement and routing；
- 生成 `.fs`；
- 生成资源和时序报告。

### 11.2 FPGA 编程

使用 `programmer_cli.exe`：

- operation 2：开发阶段 SRAM Program；
- operation 6：最终 embFlash Erase, Program, Verify；
- operation 1：从 Flash Reprogram。

### 11.3 ESP32 编译和上传

使用 Arduino IDE 自带的 `arduino-cli.exe` 后台服务：

- FQBN：`esp32:esp32:esp32`
- Arduino ESP32 core：3.3.11
- 串口：COM4

上传时大多数情况可以自动控制 EN/BOOT；出现 boot mode `0x13` 时，重试或手动按住 BOOT 进入下载模式。

### 11.4 自动硬件测试

通过 PowerShell/.NET `System.IO.Ports.SerialPort` 打开 COM4，自动发送：

```text
test N
bench N
speed M
```

并采集串口返回结果。这样可以连续重复测试，减少手动复制数据和偶然遗漏。

---

## 12. 本项目中可复用的问题解决方法

### 12.1 先建立可靠基线

不要同时修改协议、频率、算法和引脚。先在低速下证明：

- 输入正确；
- 算法正确；
- 输出正确；
- 测试工具正确。

之后所有高速错误都可以与基线比较。

### 12.2 把系统分层

本项目按以下层次隔离问题：

1. 物理线和引脚；
2. SPI 单字节/loopback；
3. SCLK 与跨时钟域；
4. 命令 parser；
5. 存储地址；
6. DSP 计算；
7. MISO 串行发送；
8. ESP32 软件协议；
9. 完整性能。

如果 loopback 已经错误，就不应该继续修改矩阵乘法器。

### 12.3 使用错误模式，而不只看“通过/失败”

- 每次都随机变化：可能是边缘时序、噪声或未初始化状态；
- 每次固定同一位错误：可能是固定 bit timing 或索引；
- 降频后完全正确：更可能是接口时序，而不是算法；
- 第一条失败、后续都通过：优先检查启动状态和同步器。

### 12.4 一次只改变一个关键变量

例如频率扫描时保持 RTL、矩阵和接线不变；修改 MISO 发送器时先在 10 MHz 验证计算核心。这样才能知道结果由哪个改动导致。

### 12.5 用器件数据和报告替代猜测

本项目的关键突破来自：

- Gowin pin database；
- clock-capable pin 标记；
- PnR 资源报告；
- `354 REG(s) unPlaced`；
- 串口逐元素差异；
- 多次重复计时。

“应该可以”不能代替 placement、routing 和硬件测试。

### 12.6 区分理论峰值、kernel 时间和端到端时间

简历项目最容易犯的错误是只计算 DSP 理论乘法次数，然后声称巨大加速。本项目使用的是完整路径时间，并明确说明 B 是否常驻。

最终可辩护的说法是：

> 在 signed INT8、signed INT32 输出、19 MHz SPI、包含 A 传输、FPGA 计算、DONE 等待和 C 回读的 resident-B 路径中，64×64 相比 ESP32 软件参考获得约 1.53× 加速。

### 12.7 保留失败版本和可靠回退

保留了：

- 原始项目；
- 100 kHz 可靠位流；
- 32×32/20 MHz 高速位流；
- 64×64/19 MHz 最终位流。

这让激进实验不会破坏已知结果，也可以解释不同架构之间的性能取舍。

---

## 13. 如何复现最终结果

### 13.1 关键文件

```text
README.md
fpga/ESP32_FPGA_4x4.gprj
fpga/src/ESP32_FPGA_4x4_64x64.v
fpga/src/ESP32_FPGA_4x4_64x64.cst
fpga/build/ESP32_FPGA_4x4.fs
fpga/build/ESP32_FPGA_4x4_64x64.rpt.txt
esp32/ESP32_FPGA_Matrix_Accelerator_64x64/
  ESP32_FPGA_Matrix_Accelerator_64x64.ino
```

### 13.2 接线

| 信号 | ESP32 | FPGA pin |
|---|---:|---:|
| SCLK | GPIO 4 | 42 |
| MISO | GPIO 18 | 40 |
| MOSI | GPIO 23 | 31 |
| CS | GPIO 5 | 39 |
| DONE | GPIO 19 | 32 |
| GND | GND | GND |

### 13.3 串口测试

波特率 115200，冷启动后运行：

```text
test 8
test 16
test 24
test 32
test 40
test 48
test 56
test 64
bench 64
```

预期：所有 `test` 通过，64×64 加速比约 1.53×。计时会有轻微波动，不应该要求每次最后三位完全相同。

---

## 14. 仍然存在的限制和建议

### 14.1 当前正确性测试的限制

目前主要使用固定、可重复的小 signed 值矩阵。这非常适合定位确定性错误，但还应该补充：

- 多个随机种子；
- signed INT8 全范围或接近边界的值；
- 连续几十次 64×64；
- 每次逐元素比较 CPU/FPGA。

建议增加 `stress 64` 命令，作为最终压力测试。

### 14.2 不建议继续追求 20 MHz

当前 19 MHz 已经有稳定正确性和明确加速。为了简历项目再追 20 MHz，收益有限，但可能引入温度、电压、线长和批次相关的不稳定。

### 14.3 不建议直接扩展到 128×128

当前 B bank 已使用 8/10 BSRAM。128×128 需要新的 K/N 分块协议、外部存储或多次加载，开发复杂度会明显增加，不适合“可靠且不过度花时间”的目标。

### 14.4 最终项目命名

建议将根目录最终改名为：

```text
ESP32_FPGA_Matrix_Accelerator_64x64
```

同时在说明中保留“8 DSP / 16 multipliers / 2×4 output tile”，避免文件名中的 4×4 历史让读者误以为最大只能计算 4×4。

---

## 15. 推荐的简历表述

可以写成：

> Designed and validated an ESP32–Gowin FPGA signed-INT8 matrix accelerator supporting matrices up to 64×64. Implemented banked BSRAM storage, CDC-safe SPI command handling, an 8-DSP parallel MAC datapath, and persistent FPGA flash boot; achieved a measured 1.53× end-to-end speedup over a 240 MHz ESP32 software baseline at 64×64, including input transfer, FPGA execution, synchronization, and result readback.

面试时应主动说明：

- 48×48 起才出现加速；
- 小矩阵受 SPI 固定开销限制；
- B resident 的假设适用于权重重复使用；
- 即使加入一次 B 加载，64×64 仍约 1.35×；
- 32×32 专用旧固件与 64×64 通用固件是两套不同架构，不能混用性能数字。

---

## 16. 最重要的工程经验

这个项目最有价值的部分，不是把一个频率数字调高，而是形成以下闭环：

```text
建立可靠基线
→ 观察可重复现象
→ 把系统分层
→ 提出可以被证伪的假设
→ 设计最小实验
→ 用硬件数据和工具报告判断
→ 保留失败证据
→ 选择有余量的最终方案
→ 做冷启动和端到端验证
```

当一个优化没有带来测量上的变化时，不应该继续相信理论推断；当一个方案只在错误数据上更快时，它不是优化；当资源报告“数量够”但布局失败时，物理实现结果优先于纸面计算。

当前版本之所以可信，不是因为它理论上应该工作，而是因为它经历了低速基线、逐层隔离、频率扫描、资源布局、完整逐元素比较、重复基准、Flash 校验和冷启动修复。
